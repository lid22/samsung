const email = document.getElementById("email");

email.addEventListener("input", function (event) {
  if (email.validity.typeMismatch) {
    email.setCustomValidity("¡Se esperaba una dirección de correo electrónico!");
  } else {
    email.setCustomValidity("");
  }


function valida_envia(){
    console.log('Formulario enviado')}
 
var nombre = document.getElementsByld("nombre")
{
    if (document.getElementByld("input")="[A-Za-z]")
    document.getElementByld("input)".style.border-color== "green");

else

document.getElementByld("input").style.border-color== "red");
}
{
    input[0].style.border-color = 'red';
    
    input[1].style.border-color = 'green';
    
}
